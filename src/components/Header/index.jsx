import Navigation from "../Nav/index";

/**
 * Header Component displays the header section of the website.
 *
 * @component
 * @returns {JSX.Element} Rendered JSX Element representing the header section of the website.
 */
export default function Header() {
  return (
    <header>
      <Navigation />
    </header>
  );
}
